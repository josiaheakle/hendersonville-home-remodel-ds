import * as React from "react";

interface IndexPageProps {}

const IndexPage: React.FC<IndexPageProps> = ({}) => {
	return (
		<main>
			<h5>need direction?</h5>
			<h1>I am a big boy</h1>
		</main>
	);
};

export default IndexPage;
